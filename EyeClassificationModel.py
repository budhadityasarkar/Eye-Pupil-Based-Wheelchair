# -*- coding: utf-8 -*-

# Form implementation generated from reading ui file 'EyeClassificationModel.ui'
#
# Created by: PyQt5 UI code generator 5.15.4
#
# WARNING: Any manual changes made to this file will be lost when pyuic5 is
# run again.  Do not edit this file unless you know what you are doing.

import cv2
from PyQt5 import QtCore, QtWidgets
from PyQt5.QtCore import pyqtSlot
from PyQt5.QtGui import QImage, QPixmap
from PyQt5.QtWidgets import QDialog, QApplication
from PyQt5.uic import loadUi
from Tracking import *


class Ui_Form(object):
    def setupUi(self, Form):
        Form.setObjectName("Form")
        Form.resize(802, 0)
        self.Face = QtWidgets.QFrame(Form)
        self.Face.setGeometry(QtCore.QRect(30, 100, 401, 401))
        self.Face.setFrameShape(QtWidgets.QFrame.StyledPanel)
        self.Face.setFrameShadow(QtWidgets.QFrame.Raised)
        self.Face.setObjectName("Face")
        self.Left = QtWidgets.QFrame(Form)
        self.Left.setGeometry(QtCore.QRect(490, 100, 131, 101))
        self.Left.setFrameShape(QtWidgets.QFrame.StyledPanel)
        self.Left.setFrameShadow(QtWidgets.QFrame.Raised)
        self.Left.setObjectName("Left")
        self.Right = QtWidgets.QFrame(Form)
        self.Right.setGeometry(QtCore.QRect(690, 100, 131, 101))
        self.Right.setFrameShape(QtWidgets.QFrame.StyledPanel)
        self.Right.setFrameShadow(QtWidgets.QFrame.Raised)
        self.Right.setObjectName("Right")
        self.lineEdit = QtWidgets.QLineEdit(Form)
        self.lineEdit.setGeometry(QtCore.QRect(340, 220, 121, 31))
        self.lineEdit.setObjectName("lineEdit")
        self.plainTextEdit = QtWidgets.QPlainTextEdit(Form)
        self.plainTextEdit.setGeometry(QtCore.QRect(340, 250, 301, 87))
        self.plainTextEdit.setObjectName("plainTextEdit")
        self.Start = QtWidgets.QPushButton(Form)
        self.Start.setGeometry(QtCore.QRect(130, 520, 101, 51))
        self.Start.setObjectName("Start")
        self.Stop = QtWidgets.QPushButton(Form)
        self.Stop.setGeometry(QtCore.QRect(670, 510, 101, 51))
        self.Stop.setObjectName("Stop")

        self.retranslateUi(Form)
        QtCore.QMetaObject.connectSlotsByName(Form)

    def retranslateUi(self, Form):
        _translate = QtCore.QCoreApplication.translate
        Form.setWindowTitle(_translate("Form", "Form"))
        self.lineEdit.setText(_translate("Form", "Result"))
        self.Stop.setText(_translate("Form", "Stop"))

    def __init__(self):
        super(Ui_Form,self).__init__()
        loadUi('EyeClassificationModel.ui',self)
        self.logic=0
        self.value=1
        self.Start.clicked.connect(self.onClicked)
        self.Stop.clicked.connect(self.close)

    @pyqtSlot()
    def onClicked(self):
        cap=cv2.VideoCapture(0)
        while (cap.isOpened()):
            ret, frame = cap.read()
            if ret == True:
                image_with_faces = faceDetection(frame)
                enlarged_image = cv2.resize(image_with_faces, (400, 400))
                eye_detected_img = eyeDetection(enlarged_image)
            self.displayImage(enlarged_image,1)
            cv2.waitKey()
        cap.release()
        cv2.destroyAllWindows()

    def displayImage(self, img, window=1):
        qformat = QImage.Format_Indexed8

        if len(img.shape) == 3:
            if(img.shape[2]) == 4:
                qformat = QImage.Format_RGBA888

            else:
                qformat = QImage.Format_RGBA888
        img = QImage(img, img.shape[1], img.shape[0], qformat)
        img = img.rgbSwapped()
        self.imgForm.setPixmap(QPixmap.fromImage(img))
        self.imgLabel.setAlignment(QtCore.Qt.AlignHCenter | QtCore.Qt.AlignVCenter)

if __name__ == "__main__":
    import sys
    app = QtWidgets.QApplication(sys.argv)
    window = Ui_Form()
    window.onClicked()
    Form = QtWidgets.QWidget()
    ui = Ui_Form()
    ui.setupUi(Form)
    Form.show()
    sys.exit(app.exec_())
